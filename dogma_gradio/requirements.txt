alphagenome
gradio>=5.0
pandas>=2.0
requests>=2.31
biopython>=1.83

# proto_tools is intentionally not pinned here because it appears to be the
# project-specific package already used in your DOGMA notebooks. Install it
# from the same repository/environment that provides:
#   ViennaRNAInput, ViennaRNAConfig, run_viennarna
#   ESM2ScoringInput, ESM2ScoringConfig, run_esm2_score
